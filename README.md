# Kubernetes-demo-with-simple-NGINX-deployments
This repository includes manifest files for a simple bird-themed Kubernetes projects.   The pods and deployments  were designed to run simple nginx containers in conjunction with config maps with customized HTML.  
